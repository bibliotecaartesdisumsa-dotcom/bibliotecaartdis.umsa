echo '[build]
  builder = "NIXPACKS"

[build.nixpacks]
  system_packages = ["ghostscript"]

[deploy]
  startCommand = "gunicorn arteydis.wsgi:application --bind 0.0.0.0:$PORT"
  healthcheckPath = "/"
  restartPolicyType = "ON_FAILURE"' > railway.toml